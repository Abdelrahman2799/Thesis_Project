/* cap input rows for the captured run */
options obs=100;

/* This bundle's script.sas is self-contained: the mock MNAR results
   table it de-duplicates is defined inline there, so the script runs
   on its own as well as through the runner. Nothing extra needed here. */
